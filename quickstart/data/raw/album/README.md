Curated table scenes live here.

Generate them from the source album with:

```bash
uv run python scripts/quickstart/compose_album_on_table.py
```

The source photos are copied to `quickstart/originals/album/` and ignored by git.
